[
    {
        "name": "sublist3r",
        "description": "sublist3r is security tool designed to enumerate subdomains of websites using OSINT.",
        "check_command": "sublist3r --version",
        "installation": {
            "linux": ["apt install sublist3r -y"]
        }
    },
    {
        "name": "subfinder",
        "description": "subfinder is a subdomain discovery tool that discovers valid subdomains for websites.",
        "check_command": "subfinder --version",
        "installation": {
            "linux": ["apt install subfinder -y"]
        }
    },
    {
        "name": "assetfinder",
        "description": "assetfinder is a tool designed to find domains and subdomains associated with a specific domain.",
        "check_command": "assetfinder --version",
        "installation": {
            "linux": ["apt install assetfinder -y"]
        }
    },
    {
        "name": "gobuster",
        "description": "Gobuster is a tool used to brute-force: URIs (directories and files) in web sites.",
        "check_command": "gobuster version",
        "installation": {
            "linux": ["apt install gobuster -y"]
        }
    },

    {
        "name": "nuclei",
        "description": "Nuclei is used to send requests across targets based on a template.",
        "check_command": "nuclei --version",
        "installation": {
            "linux": ["apt install nuclei -y"]
        }
    },

    {
        "name": "katana",
        "description": "In-depth DNS enumeration and network mapping.",
        "check_command": "katana -version",
        "installation": {
            "linux": [
                "apt install golang-go -y",
                "go install github.com/projectdiscovery/katana/cmd/katana@latest"
            ]
        },
        "post_install": {
            "linux": ["cp ~/go/bin/katana /usr/local/bin/"]
        }
    },

    {
        "name": "waybackurls",
        "description": "Fetch all the URLs that the Wayback Machine knows about for a domain.",
        "check_command": "waybackurls -h",
        "installation": {
            "linux": [
                "apt install golang-go -y",
                "go install github.com/tomnomnom/waybackurls@latest"
            ]
        },
        "post_install": {
            "linux": ["cp ~/go/bin/waybackurls /usr/local/bin/"]
        }
    }





]
